#include <stdio.h>

/**
* main - prints  number of arguments,
* @argc: number of command line  arguments,
* @argv: pointer to an array of command line arguments.
* Return: 0 (Success) ,  none-zero (Failure).
*/

int main(int argc, char const *argv[])

{
	(void)argv;
	printf("%d\n", (arg - 1));
	return (0);
}
